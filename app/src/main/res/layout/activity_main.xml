<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="ca.uqac.liara.imurecording.Activities.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:background="@color/colorPrimary"
        app:titleTextColor="@color/colorPrimaryIconsText"
        android:layout_height="?attr/actionBarSize" />

    <include
        android:id="@+id/layout_device"
        layout="@layout/content_device" />

    <include
        android:id="@+id/layout_communication"
        android:visibility="gone"
        layout="@layout/content_communication" />

</RelativeLayout>
